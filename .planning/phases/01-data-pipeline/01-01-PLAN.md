---
phase: 01-data-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/__init__.py
  - src/data/inspect_dream.py
  - src/utils/__init__.py
  - src/utils/logging.py
  - tests/__init__.py
  - tests/test_inspect_dream.py
  - scripts/inspect_dream.py
  - data/processed/dream/DATASET_CARD.md
  - data/processed/dream/inspection_report.json
  - configs/inspect_dream.yaml
autonomous: true
requirements: [INSP-01, INSP-02, INSP-03, INSP-04]
must_haves:
  truths:
    - "Running `python scripts/inspect_dream.py` produces a directory listing of data/raw/dream with per-extension counts"
    - "EEG signal properties (sfreq, n_channels, channel types, duration, annotations) are logged for at least one subject file"
    - "Dream report structure is documented (per-subject file pattern, free-text vs structured, average length)"
    - "data/processed/dream/DATASET_CARD.md exists and documents format, signal properties, subject/session counts, label structure, and known issues"
  artifacts:
    - path: "src/data/inspect_dream.py"
      provides: "Dataset audit, format identification, EEG signal inspection, report inspection, subject structure analysis"
    - path: "scripts/inspect_dream.py"
      provides: "CLI entry point that runs inspection end-to-end and writes DATASET_CARD.md"
    - path: "data/processed/dream/DATASET_CARD.md"
      provides: "Dataset card with format, signal properties, subject counts, label structure, known issues"
    - path: "data/processed/dream/inspection_report.json"
      provides: "Machine-readable inspection results consumed by later plans (sfreq, n_channels, ch_names, duration, annotations, subject list, report format)"
    - path: "configs/inspect_dream.yaml"
      provides: "Configurable raw data root and output paths"
    - path: "tests/test_inspect_dream.py"
      provides: "Unit test covering inspection happy path, empty-directory edge case, and missing-file failure case"
  key_links:
    - from: "scripts/inspect_dream.py"
      to: "src/data/inspect_dream.py"
      via: "imports run_inspection()"
      pattern: "from src.data.inspect_dream import"
    - from: "src/data/inspect_dream.py"
      to: "data/processed/dream/DATASET_CARD.md"
      via: "writes markdown with documented sections"
      pattern: "DATASET_CARD"
    - from: "src/data/inspect_dream.py"
      to: "mne.io.read_raw_edf"
      via: "MNE EEG loader"
      pattern: "mne\\.io\\.read_raw"
---

<objective>
Inspect the DREAM dataset and produce an authoritative dataset card plus a machine-readable inspection report that preprocessing plans (02) and alignment plans (03) consume. This plan MUST run before any preprocessing code is written because the dataset format is unknown a priori (AGENTS.md Dataset Inspection Protocol).

Purpose: Resolve the blocker documented in STATE.md ("DREAM dataset file format is unknown until inspection") and establish the canonical record of signal properties, subject/session counts, and report structure.
Output: DATASET_CARD.md, inspection_report.json, reusable inspection module, CLI script, and tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Shared logging utility and inspection config</name>
  <files>src/utils/__init__.py, src/utils/logging.py, configs/inspect_dream.yaml, tests/__init__.py</files>
  <read_first>
    - AGENTS.md (sections: Agent Discipline Rules rule #6 "Log shapes at every stage", Stack & Repository Structure)
    - .planning/ROADMAP.md (Phase 1 success criteria)
  </read_first>
  <behavior>
    - `src.utils.logging.get_logger(name: str) -> logging.Logger` returns a logger configured at INFO level with format `"%(asctime)s | %(levelname)s | %(name)s | %(message)s"`
    - Calling `get_logger("x")` twice returns loggers that do not duplicate handlers (idempotent handler setup)
    - `configs/inspect_dream.yaml` loads as valid YAML with keys `raw_root`, `output_card`, `output_report`
  </behavior>
  <action>
    Create `src/utils/__init__.py` (empty), `src/utils/logging.py` with a `get_logger(name)` function that attaches a single `StreamHandler` at INFO level using format `"%(asctime)s | %(levelname)s | %(name)s | %(message)s"`. Guard handler attachment with `if not logger.handlers:` so repeated calls do not duplicate handlers. Do NOT use `print` anywhere in src/.

    Create `configs/inspect_dream.yaml` with exactly these keys and values:
    ```yaml
    raw_root: data/raw/dream
    output_card: data/processed/dream/DATASET_CARD.md
    output_report: data/processed/dream/inspection_report.json
    max_files_to_probe: 3
    ```

    Create `tests/__init__.py` (empty) and `src/data/__init__.py` (empty) so pytest discovers packages. Create `src/__init__.py` if missing.
  </action>
  <verify>
    <automated>python -c "from src.utils.logging import get_logger; l1=get_logger('t'); l2=get_logger('t'); assert len(l1.handlers)==1 and l1 is l2" && python -c "import yaml; c=yaml.safe_load(open('configs/inspect_dream.yaml')); assert c['raw_root']=='data/raw/dream' and c['output_card'].endswith('DATASET_CARD.md')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "def get_logger" src/utils/logging.py` succeeds
    - `grep -q "logging.INFO" src/utils/logging.py` succeeds
    - `grep -q "if not logger.handlers" src/utils/logging.py` succeeds (idempotent handler guard)
    - `! grep -rn "print(" src/utils/ src/data/` (no print statements in src)
    - `test -f configs/inspect_dream.yaml && grep -q "raw_root: data/raw/dream" configs/inspect_dream.yaml`
    - `test -f src/utils/__init__.py && test -f src/data/__init__.py && test -f tests/__init__.py`
  </acceptance_criteria>
  <done>Logging utility + inspection config exist, no print usage in src/, package __init__ files present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Inspection module + tests (audit, probe EEG, probe reports, subject structure)</name>
  <files>src/data/inspect_dream.py, tests/test_inspect_dream.py</files>
  <read_first>
    - AGENTS.md (Dataset Inspection Protocol, Steps 1-6)
    - src/utils/logging.py (from Task 1)
    - configs/inspect_dream.yaml (from Task 1)
  </read_first>
  <behavior>
    - `audit_directory(root: Path) -> dict` returns `{"tree": List[str], "ext_counts": Dict[str, int], "total_files": int}`
    - `probe_eeg_file(path: Path) -> dict` returns `{"path": str, "sfreq": float, "n_channels": int, "ch_names": List[str], "ch_types": List[str], "duration_s": float, "annotations": List[dict]}` using MNE
    - `probe_report_files(root: Path, pattern: str) -> dict` returns `{"n_reports": int, "example_path": str|None, "avg_length_chars": float, "example_snippet": str|None}`
    - `discover_subjects(root: Path) -> List[str]` returns sorted list of subject IDs derived from filenames matching `sub-<id>` pattern
    - `run_inspection(config: dict) -> dict` orchestrates the above and returns a dict with keys: `audit`, `eeg_sample`, `reports`, `subjects`, `known_issues`
    - `write_dataset_card(inspection: dict, output_path: Path)` writes a markdown file with the exact section headers below
    - `write_inspection_report(inspection: dict, output_path: Path)` writes valid JSON
    - Empty directory: `audit_directory` returns `{"tree": [], "ext_counts": {}, "total_files": 0}` without raising
    - Missing EEG file: `probe_eeg_file` raises `FileNotFoundError`
  </behavior>
  <action>
    Create `src/data/inspect_dream.py` implementing the functions above. Use `mne.io.read_raw_edf(path, preload=False, verbose=False)` for EEG probing; if the file extension is `.set` use `mne.io.read_raw_eeglab`, if `.fif` use `mne.io.read_raw_fif`, if `.vhdr` use `mne.io.read_raw_brainvision`. Auto-dispatch on suffix inside a helper `_load_raw(path)`.

    `audit_directory` must walk with `Path.rglob("*")`, build a tree list (indented by depth, capped at depth 3), and count file extensions via `collections.Counter`.

    `probe_report_files` looks for files matching any of these patterns: `sub-*_dream.txt`, `sub-*_report.txt`, `*.tsv`, `*.csv`, `*.json` under the root. Return the first matching pattern's stats. If none found, set `n_reports=0`, `example_path=None`.

    `discover_subjects` uses regex `r"sub-([A-Za-z0-9]+)"` against filenames and returns the sorted unique set.

    `run_inspection` calls all probes, catches exceptions from individual probes and records them as strings in `known_issues`. It MUST pick up to `config["max_files_to_probe"]` EEG files (first three by sorted path) for probing and store the first successful probe under `eeg_sample`.

    `write_dataset_card` MUST emit a markdown file with EXACTLY these H2 headers in this order:
    ```
    # DREAM Dataset Card

    ## Format
    ## Signal Properties
    ## Subject and Session Counts
    ## Label Structure
    ## Known Issues
    ```
    Under each header, write the relevant fields from the inspection dict. Under `## Signal Properties` include the strings `sfreq:`, `n_channels:`, `duration_s:`, `annotations:`. Under `## Subject and Session Counts` include `n_subjects:` followed by `len(inspection["subjects"])`.

    All functions use `get_logger(__name__)` from `src.utils.logging` and log shape/size information at INFO level as required by AGENTS.md rule #6.

    Create `tests/test_inspect_dream.py` with these tests:
    1. `test_audit_directory_empty(tmp_path)` — creates empty dir, asserts `total_files == 0` and `ext_counts == {}`
    2. `test_audit_directory_counts_extensions(tmp_path)` — creates `a.edf`, `b.edf`, `c.txt`, asserts `ext_counts == {".edf": 2, ".txt": 1}`
    3. `test_probe_eeg_file_missing_raises(tmp_path)` — asserts `FileNotFoundError` on non-existent path
    4. `test_discover_subjects_parses_sub_ids(tmp_path)` — creates files `sub-01_eeg.edf`, `sub-02_eeg.edf`, `sub-01_dream.txt`, asserts result is `["01", "02"]`
    5. `test_write_dataset_card_contains_required_sections(tmp_path)` — calls `write_dataset_card` with a minimal dict, asserts output file contains each of the five H2 headers listed above
    6. `test_write_inspection_report_is_valid_json(tmp_path)` — asserts written file parses with `json.loads`
    7. `test_run_inspection_records_known_issues_on_probe_failure(tmp_path, monkeypatch)` — forces `_load_raw` to raise and asserts `known_issues` is non-empty
  </action>
  <verify>
    <automated>python -m pytest tests/test_inspect_dream.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "def audit_directory" src/data/inspect_dream.py`
    - `grep -q "def probe_eeg_file" src/data/inspect_dream.py`
    - `grep -q "def probe_report_files" src/data/inspect_dream.py`
    - `grep -q "def discover_subjects" src/data/inspect_dream.py`
    - `grep -q "def run_inspection" src/data/inspect_dream.py`
    - `grep -q "def write_dataset_card" src/data/inspect_dream.py`
    - `grep -q "def write_inspection_report" src/data/inspect_dream.py`
    - `grep -q "mne.io.read_raw" src/data/inspect_dream.py`
    - `grep -q "from src.utils.logging import get_logger" src/data/inspect_dream.py`
    - `! grep -n "print(" src/data/inspect_dream.py`
    - `python -m pytest tests/test_inspect_dream.py -q` exits 0 with ≥ 7 tests collected
  </acceptance_criteria>
  <done>All 7 unit tests pass; inspection module exposes audit/probe/write functions; uses MNE and project logger exclusively.</done>
</task>

<task type="auto">
  <name>Task 3: CLI runner that produces DATASET_CARD.md and inspection_report.json</name>
  <files>scripts/inspect_dream.py, data/processed/dream/DATASET_CARD.md, data/processed/dream/inspection_report.json</files>
  <read_first>
    - src/data/inspect_dream.py (from Task 2)
    - configs/inspect_dream.yaml (from Task 1)
    - AGENTS.md (Dataset Inspection Protocol Step 6 — Dataset Card sections)
  </read_first>
  <action>
    Create `scripts/inspect_dream.py` that:
    1. Parses `--config configs/inspect_dream.yaml` via `argparse` (default value set to that path).
    2. Loads the YAML with `yaml.safe_load`.
    3. Calls `run_inspection(config)`.
    4. Ensures `Path(config["output_card"]).parent` exists with `mkdir(parents=True, exist_ok=True)`.
    5. Calls `write_dataset_card(inspection, Path(config["output_card"]))`.
    6. Calls `write_inspection_report(inspection, Path(config["output_report"]))`.
    7. Logs final paths at INFO via `get_logger(__name__)`.
    8. Exits 0 on success.

    If `data/raw/dream/` does NOT exist on this machine (common — raw data may not yet be downloaded), the CLI MUST still produce a valid DATASET_CARD.md and inspection_report.json. In that case, `run_inspection` will return empty audit + zero subjects, and `known_issues` will contain the string `"raw_root_missing: data/raw/dream"`. The dataset card must still contain all five required H2 sections and note the missing raw data under `## Known Issues`.

    Then run the CLI from the project root: `python scripts/inspect_dream.py` — this will create the two output files.
  </action>
  <verify>
    <automated>python scripts/inspect_dream.py && test -f data/processed/dream/DATASET_CARD.md && test -f data/processed/dream/inspection_report.json && python -c "import json; d=json.load(open('data/processed/dream/inspection_report.json')); assert 'audit' in d and 'subjects' in d and 'known_issues' in d"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f scripts/inspect_dream.py`
    - `grep -q "argparse" scripts/inspect_dream.py`
    - `grep -q "from src.data.inspect_dream import" scripts/inspect_dream.py`
    - `test -f data/processed/dream/DATASET_CARD.md`
    - `grep -q "^# DREAM Dataset Card" data/processed/dream/DATASET_CARD.md`
    - `grep -q "^## Format" data/processed/dream/DATASET_CARD.md`
    - `grep -q "^## Signal Properties" data/processed/dream/DATASET_CARD.md`
    - `grep -q "^## Subject and Session Counts" data/processed/dream/DATASET_CARD.md`
    - `grep -q "^## Label Structure" data/processed/dream/DATASET_CARD.md`
    - `grep -q "^## Known Issues" data/processed/dream/DATASET_CARD.md`
    - `test -f data/processed/dream/inspection_report.json`
    - `python -c "import json; d=json.load(open('data/processed/dream/inspection_report.json')); assert all(k in d for k in ['audit','eeg_sample','reports','subjects','known_issues'])"` exits 0
  </acceptance_criteria>
  <done>CLI runs to completion, both output files exist, dataset card contains all five required sections, inspection report is valid JSON with all top-level keys.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_inspect_dream.py -q` passes
- `python scripts/inspect_dream.py` exits 0
- `data/processed/dream/DATASET_CARD.md` exists with all five H2 sections (INSP-04)
- `data/processed/dream/inspection_report.json` exists with keys `audit`, `eeg_sample`, `reports`, `subjects`, `known_issues` (INSP-01, INSP-02, INSP-03)
- No `print(` statements in `src/`
</verification>

<success_criteria>
INSP-01: Directory listing + ext counts recorded in inspection_report.audit.
INSP-02: EEG signal properties (sfreq, n_channels, ch_types, duration, annotations) recorded in inspection_report.eeg_sample.
INSP-03: Report structure recorded in inspection_report.reports (or known_issue if none found).
INSP-04: DATASET_CARD.md written with Format / Signal Properties / Subject and Session Counts / Label Structure / Known Issues sections.
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-pipeline/01-01-SUMMARY.md` using the summary template.
</output>
</content>
</invoke>