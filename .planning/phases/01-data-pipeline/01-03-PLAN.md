---
phase: 01-data-pipeline
plan: 03
type: execute
wave: 3
depends_on: [01-01, 01-02]
files_modified:
  - src/data/align_reports.py
  - src/data/make_splits.py
  - tests/test_align_reports.py
  - tests/test_make_splits.py
  - scripts/align_reports.py
  - scripts/make_splits.py
  - configs/align_reports.yaml
  - configs/make_splits.yaml
autonomous: true
requirements: [ALGN-01, ALGN-02, ALGN-03, ALGN-04]
must_haves:
  truths:
    - "Each subject's dream report is encoded into a 384-dim all-MiniLM-L6-v2 embedding"
    - "Awakening timestamps are identified from EEG annotations and REM epochs within 30s before awakening are selected as training targets"
    - "data/processed/dream/sub-<id>_target_embeddings.npz exists per subject with epoch_indices, target_embeddings (float32, 384-dim), and report_text"
    - "data/splits/dream_splits.json exists with train/val/test subject lists and no subject appears in more than one set"
  artifacts:
    - path: "src/data/align_reports.py"
      provides: "Report loading, sentence-transformer encoding, awakening detection, REM-window selection, target save"
    - path: "src/data/make_splits.py"
      provides: "Subject-level train/val/test split generator"
    - path: "tests/test_align_reports.py"
      provides: "Unit tests for encoding shape, REM window selection, output file structure"
    - path: "tests/test_make_splits.py"
      provides: "Unit tests for split disjointness, ratio correctness, determinism"
    - path: "scripts/align_reports.py"
      provides: "CLI that runs alignment for all subjects"
    - path: "scripts/make_splits.py"
      provides: "CLI that writes data/splits/dream_splits.json"
    - path: "configs/align_reports.yaml"
      provides: "embedding_model, embedding_dim, time_alignment_window, report_file_pattern"
    - path: "configs/make_splits.yaml"
      provides: "train_ratio, val_ratio, test_ratio, seed"
  key_links:
    - from: "src/data/align_reports.py"
      to: "sentence_transformers.SentenceTransformer('all-MiniLM-L6-v2')"
      via: "report encoding"
      pattern: "SentenceTransformer"
    - from: "src/data/align_reports.py"
      to: "data/processed/dream/eeg/sub-*_epochs.npz"
      via: "loads epochs to find REM indices and epoch_times_s"
      pattern: "np\\.load.*epochs\\.npz"
    - from: "src/data/make_splits.py"
      to: "data/splits/dream_splits.json"
      via: "json.dump of {train:[...], val:[...], test:[...]}"
      pattern: "dream_splits\\.json"
---

<objective>
Implement semantic alignment (Agent 1B in AGENTS.md) and subject-level dataset splits. Transform dream reports into 384-dim embeddings aligned to REM epochs, and partition subjects disjointly into train/val/test.

Purpose: Close Phase 1 by producing the final two deliverables required for Phase 2 training.
Output: Per-subject target embedding files + `data/splits/dream_splits.json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/REQUIREMENTS.md
@src/data/preprocess_dream.py
@data/processed/dream/DATASET_CARD.md
@src/utils/logging.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Report alignment module + tests + CLI + config</name>
  <files>src/data/align_reports.py, tests/test_align_reports.py, scripts/align_reports.py, configs/align_reports.yaml</files>
  <read_first>
    - AGENTS.md (Agent 1B — Dream Report Aligner section: Config schema, Processing steps, Edge cases)
    - src/data/preprocess_dream.py (from Plan 01-02 — defines the `sub-<id>_epochs.npz` format this module reads)
    - src/utils/logging.py
  </read_first>
  <behavior>
    - `encode_report(text: str, model) -> np.ndarray` returns float32 array of shape `(384,)`
    - `encode_reports_batch(texts: List[str], model) -> np.ndarray` returns shape `(len(texts), 384)` float32
    - `find_awakening_times(annotations: List[dict], recording_end_s: float) -> List[float]` returns list of times marked as awakenings; any annotation whose description (lowercased) contains "awaken", "wake_up", "arousal_end" counts; if none found, returns `[recording_end_s]`
    - `select_rem_epochs_before_awakening(sleep_stages: np.ndarray, epoch_times_s: np.ndarray, awakening_time_s: float, window_s: float) -> np.ndarray` returns integer indices where `sleep_stages == 4` AND `awakening_time_s - window_s <= epoch_times_s[i] < awakening_time_s`
    - `load_subject_epochs(subject_npz_path: Path) -> dict` returns a dict with keys `sleep_stages`, `epoch_times_s`, `subject_id`, `annotations` (if stored)
    - `align_subject(subject_id, epochs_path, report_path, model, cfg) -> dict | None` returns `{"epoch_indices", "target_embeddings", "report_text"}` or `None` if no REM epochs fall in the window
    - `save_target_embeddings(result: dict, output_path: Path) -> None` saves `.npz` with keys `epoch_indices` (int64), `target_embeddings` (float32, shape `(k, 384)`), `report_text` (str)
  </behavior>
  <action>
    Create `configs/align_reports.yaml`:
    ```yaml
    epochs_dir: data/processed/dream/eeg/
    reports_root: data/raw/dream/
    report_file_pattern: "sub-*_dream.txt"
    embedding_model: "all-MiniLM-L6-v2"
    embedding_dim: 384
    time_alignment_window: 30.0
    output_dir: data/processed/dream/
    ```

    Create `src/data/align_reports.py` implementing the functions above. Use `from sentence_transformers import SentenceTransformer`. In `encode_report`, call `model.encode(text, convert_to_numpy=True)` and cast to float32. Assert `embedding.shape == (384,)`.

    `find_awakening_times` iterates the annotations list (each entry is a dict with `onset` and `description`). Use substring matching on lowercased descriptions: `any(kw in desc.lower() for kw in ("awaken", "wake_up", "arousal_end"))`.

    `select_rem_epochs_before_awakening` uses boolean masking: `mask = (sleep_stages == 4) & (epoch_times_s >= awakening_time_s - window_s) & (epoch_times_s < awakening_time_s)`; return `np.where(mask)[0].astype(np.int64)`.

    `align_subject` loads the epoch `.npz`, reads `sleep_stages` and `epoch_times_s`, loads the matching report file (`reports_root/sub-<id>_dream.txt`), handles missing report files by returning None with a logged warning, encodes the report once, then for each awakening time collects indices via `select_rem_epochs_before_awakening`. Concatenate indices across awakenings (unique + sorted). If empty → return None. Broadcast the single report embedding to shape `(len(indices), 384)` via `np.broadcast_to(emb, (len(indices), 384)).copy().astype(np.float32)`.

    `save_target_embeddings` uses `np.savez` with the three keys.

    Use `get_logger(__name__)` at INFO level for: model load, per-subject n_epochs selected, output path.

    Create `scripts/align_reports.py` CLI:
    1. Parses `--config configs/align_reports.yaml`
    2. Loads the sentence transformer ONCE at startup
    3. Globs `cfg["epochs_dir"]/sub-*_epochs.npz` to find subjects
    4. For each, calls `align_subject`; if result is not None, saves `data/processed/dream/sub-<id>_target_embeddings.npz`
    5. If no epoch files exist (Plan 01-02 did not run because raw data missing), logs a warning and exits 0
    6. Creates output dir with `mkdir(parents=True, exist_ok=True)`

    Create `tests/test_align_reports.py` with these tests (use an offline stub for SentenceTransformer — define a `DummyModel` class with `.encode(text, convert_to_numpy=True)` returning `np.zeros(384, dtype=np.float32)` and inject via function parameter, NOT monkeypatching the real model):
    1. `test_encode_report_returns_384_float32()` — use DummyModel, assert shape `(384,)` and dtype float32
    2. `test_encode_reports_batch_shape()` — encode 3 texts, assert `(3, 384)`
    3. `test_find_awakening_times_matches_keywords()` — annotations `[{"onset": 100.0, "description": "awakening"}, {"onset": 200.0, "description": "arousal_end"}, {"onset": 300.0, "description": "other"}]`, assert returned list is `[100.0, 200.0]`
    4. `test_find_awakening_times_falls_back_to_recording_end()` — annotations with no keywords, assert returned list equals `[recording_end_s]`
    5. `test_select_rem_epochs_before_awakening_boundaries()` — stages `[0,4,4,4,4,0]`, epoch_times `[0,10,20,50,80,100]`, awakening=90, window=30, assert indices `[3, 4]` (only 50<=t<90 and stage==4 → 50 and 80)
    6. `test_select_rem_epochs_empty_when_no_rem_in_window()` — assert returned array has shape `(0,)` and dtype int64
    7. `test_save_target_embeddings_roundtrip(tmp_path)` — save and reload via `np.load`, assert all three keys present, `target_embeddings.dtype == np.float32`, `target_embeddings.shape[1] == 384`
    8. `test_align_subject_returns_none_when_no_rem_in_window(tmp_path)` — build a fake epoch npz with no REM epochs in window, call `align_subject`, assert returns None
  </action>
  <verify>
    <automated>python -m pytest tests/test_align_reports.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "def encode_report" src/data/align_reports.py`
    - `grep -q "def find_awakening_times" src/data/align_reports.py`
    - `grep -q "def select_rem_epochs_before_awakening" src/data/align_reports.py`
    - `grep -q "def align_subject" src/data/align_reports.py`
    - `grep -q "def save_target_embeddings" src/data/align_reports.py`
    - `grep -q "sentence_transformers" src/data/align_reports.py`
    - `grep -q "all-MiniLM-L6-v2" configs/align_reports.yaml`
    - `grep -q "embedding_dim: 384" configs/align_reports.yaml`
    - `grep -q "time_alignment_window: 30" configs/align_reports.yaml`
    - `grep -q "sleep_stages == 4" src/data/align_reports.py`
    - `grep -q "np.float32" src/data/align_reports.py`
    - `grep -q "from src.utils.logging import get_logger" src/data/align_reports.py`
    - `! grep -n "print(" src/data/align_reports.py`
    - `python -m pytest tests/test_align_reports.py -q` exits 0 with ≥ 8 tests collected
    - `test -f scripts/align_reports.py && grep -q "argparse" scripts/align_reports.py`
    - `python scripts/align_reports.py --config configs/align_reports.yaml` exits 0 (may log "no epoch files" warning if raw data absent)
  </acceptance_criteria>
  <done>Alignment module + tests + CLI + config exist; tests pass using a DummyModel stub (no network); CLI exits 0 even when raw/preprocessed data is absent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Subject-level split generator + tests + CLI + config</name>
  <files>src/data/make_splits.py, tests/test_make_splits.py, scripts/make_splits.py, configs/make_splits.yaml, data/splits/dream_splits.json</files>
  <read_first>
    - AGENTS.md (What Not To Do: "Do not pool subjects" and "Do not use validation subjects for any training step")
    - src/data/preprocess_dream.py (defines where epoch files live)
    - .planning/REQUIREMENTS.md (ALGN-04)
  </read_first>
  <behavior>
    - `discover_subject_ids(epochs_dir: Path) -> List[str]` returns sorted list of subject IDs from filenames matching `sub-<id>_epochs.npz`
    - `make_splits(subject_ids: List[str], train_ratio: float, val_ratio: float, test_ratio: float, seed: int) -> dict` returns `{"train": [...], "val": [...], "test": [...]}` with:
      - ratios summing to 1.0 (±1e-6) or raises ValueError
      - no subject appearing in more than one set
      - deterministic given the same seed
      - train size = `floor(n * train_ratio)`, val size = `floor(n * val_ratio)`, test gets the remainder
    - `save_splits(splits: dict, output_path: Path) -> None` writes JSON with `indent=2`
  </behavior>
  <action>
    Create `configs/make_splits.yaml`:
    ```yaml
    epochs_dir: data/processed/dream/eeg/
    output_path: data/splits/dream_splits.json
    train_ratio: 0.7
    val_ratio: 0.15
    test_ratio: 0.15
    seed: 42
    ```

    Create `src/data/make_splits.py` implementing the functions. In `make_splits`:
    ```python
    rng = np.random.default_rng(seed)
    shuffled = list(subject_ids)
    rng.shuffle(shuffled)
    n = len(shuffled)
    n_train = int(n * train_ratio)
    n_val = int(n * val_ratio)
    train = sorted(shuffled[:n_train])
    val = sorted(shuffled[n_train:n_train + n_val])
    test = sorted(shuffled[n_train + n_val:])
    ```
    Validate disjointness with `assert set(train).isdisjoint(val) and set(train).isdisjoint(test) and set(val).isdisjoint(test)`.

    Create `scripts/make_splits.py` CLI:
    1. Parses `--config configs/make_splits.yaml`
    2. Calls `discover_subject_ids`; if empty (Plan 01-02 did not run), writes a placeholder `data/splits/dream_splits.json` with `{"train": [], "val": [], "test": [], "note": "No preprocessed subjects found — rerun after Plan 01-02 on a machine with raw data"}` and exits 0
    3. Otherwise calls `make_splits` and `save_splits`
    4. Creates `data/splits/` with `mkdir(parents=True, exist_ok=True)`
    5. Run it once as part of this task so `data/splits/dream_splits.json` exists

    Create `tests/test_make_splits.py`:
    1. `test_make_splits_disjoint()` — subjects `["01","02","03","04","05","06","07","08","09","10"]`, ratios 0.7/0.15/0.15, assert `set(train) & set(val) == set()` etc. for all pairs
    2. `test_make_splits_deterministic()` — call twice with seed=42, assert equal results
    3. `test_make_splits_coverage()` — assert `set(train) | set(val) | set(test) == set(subjects)` (every subject used exactly once)
    4. `test_make_splits_ratios()` — 100 subjects, 0.7/0.15/0.15, assert `len(train)==70`, `len(val)==15`, `len(test)==15`
    5. `test_make_splits_invalid_ratios_raises()` — 0.5/0.3/0.3 → raises ValueError
    6. `test_save_splits_writes_valid_json(tmp_path)` — save and reload with `json.load`, assert keys `train`, `val`, `test` are present and values are lists
    7. `test_discover_subject_ids_filters_correctly(tmp_path)` — create files `sub-01_epochs.npz`, `sub-02_epochs.npz`, `sub-01_target_embeddings.npz`, `foo.txt`; assert result is `["01", "02"]`
  </action>
  <verify>
    <automated>python -m pytest tests/test_make_splits.py -x -q && python scripts/make_splits.py --config configs/make_splits.yaml && test -f data/splits/dream_splits.json && python -c "import json; d=json.load(open('data/splits/dream_splits.json')); assert set(['train','val','test']).issubset(d.keys()); t=set(d['train']); v=set(d['val']); te=set(d['test']); assert t.isdisjoint(v) and t.isdisjoint(te) and v.isdisjoint(te)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "def discover_subject_ids" src/data/make_splits.py`
    - `grep -q "def make_splits" src/data/make_splits.py`
    - `grep -q "def save_splits" src/data/make_splits.py`
    - `grep -q "np.random.default_rng" src/data/make_splits.py`
    - `! grep -n "print(" src/data/make_splits.py`
    - `grep -q "train_ratio: 0.7" configs/make_splits.yaml`
    - `grep -q "val_ratio: 0.15" configs/make_splits.yaml`
    - `grep -q "test_ratio: 0.15" configs/make_splits.yaml`
    - `grep -q "seed: 42" configs/make_splits.yaml`
    - `python -m pytest tests/test_make_splits.py -q` exits 0 with ≥ 7 tests collected
    - `test -f data/splits/dream_splits.json`
    - `python -c "import json; d=json.load(open('data/splits/dream_splits.json')); assert 'train' in d and 'val' in d and 'test' in d"` exits 0
    - Disjointness verified via the one-liner in `<verify>`
  </acceptance_criteria>
  <done>Splits module + tests + CLI + config exist, all tests pass, `data/splits/dream_splits.json` exists on disk with disjoint train/val/test keys.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_align_reports.py tests/test_make_splits.py -q` passes
- `python scripts/align_reports.py --config configs/align_reports.yaml` exits 0
- `python scripts/make_splits.py --config configs/make_splits.yaml` exits 0
- `data/splits/dream_splits.json` exists and contains disjoint train/val/test lists
- When preprocessed epoch files are present on disk, `data/processed/dream/sub-<id>_target_embeddings.npz` files are produced with the three required keys
</verification>

<success_criteria>
ALGN-01: Dream reports encoded to 384-dim embeddings via all-MiniLM-L6-v2.
ALGN-02: Awakening times identified from annotations; REM epochs within 30s before awakening selected.
ALGN-03: Per-subject `sub-<id>_target_embeddings.npz` written with `epoch_indices`, `target_embeddings` (float32 384-dim), and `report_text`.
ALGN-04: `data/splits/dream_splits.json` written with subject-level disjoint train/val/test splits.
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-pipeline/01-03-SUMMARY.md`.
</output>
</content>
</invoke>